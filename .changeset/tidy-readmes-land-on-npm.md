---
"@variax-ai/video-schema": patch
"@variax-ai/video-renderer": patch
"@variax-ai/video-extractor": patch
---

Add package READMEs. npm ships README.md regardless of `files`, so this is what
makes each package's npm page show something other than "no description".
